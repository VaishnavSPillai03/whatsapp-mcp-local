# Your WhatsApp data and session credentials.
# data/auth/ can read and send as you — never commit it, never sync it to
# cloud storage.
data/

# Ad-hoc outreach scripts. These embed real names and phone numbers, so they must
# never reach a public repo.
outreach/

node_modules/

# Backups written by src/setup.js
*.backup

# OS noise
.DS_Store
Thumbs.db

# Build output
dist/

server/licences.json
